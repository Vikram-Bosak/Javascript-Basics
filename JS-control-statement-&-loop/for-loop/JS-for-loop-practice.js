// => JavaScript program to print table for give number ( 8,9,12,15) using for loop?

for(var num =1; num <= 10; num++){
    var tableof = 12;
    console.log(tableof + " * " + num + " = " + tableof * num);

}
// => condiional (ternary) operator

// -> codiitional (ternary) operator is the only javascript operator that takes three operands

// => this is example of else:grinning  ⬇️

var age = 19;

if (age >= 18) {
    console.log('you are  eligible to vote');

} else {
console.log('you are not eligible to vote');

}

// => this is a ternary operator examples . ant it's to short from ifelse  ⬇️

var age = 17;
console.log((age >= 18 ) ? "you can vote" : "you can't vote");
